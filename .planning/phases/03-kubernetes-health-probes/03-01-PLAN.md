---
phase: 03-kubernetes-health-probes
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Health probes are configured using /q/health/live and /q/health/ready endpoints"
    - "Container resource limits and requests are configurable via values.yaml"
    - "Probe timing parameters are configurable via values.yaml"
  artifacts:
    - path: "chart/templates/deployment.yaml"
      provides: "Kubernetes deployment with health probes"
      contains: "livenessProbe"
      contains: "readinessProbe"
      contains: "/q/health/live"
      contains: "/q/health/ready"
    - path: "chart/values.yaml"
      provides: "Helm values with configurable resources"
      contains: "resources:"
      contains: "livenessProbe:"
      contains: "readinessProbe:"
  key_links:
    - from: "chart/templates/deployment.yaml"
      to: "chart/values.yaml"
      via: " Helm templating {{ .Values.livenessProbe.* }}"
      pattern: "\\{\\{ \\.Values\\.livenessProbe"
---

<objective>
Verify that Kubernetes health probes and configurable resources are already implemented in Phase 1's Helm chart.

Purpose: Confirm requirements HELM-03 and HELM-04 are satisfied without additional implementation work.
Output: Phase completion summary and roadmap update
</objective>

<execution_context>
@/home/arkadius/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/arkadius/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-kubernetes-health-probes/03-RESEARCH.md
@chart/templates/deployment.yaml
@chart/values.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify health probe implementation in deployment.yaml</name>
  <files>chart/templates/deployment.yaml</files>
  <action>
    Read chart/templates/deployment.yaml and verify:
    1. livenessProbe exists with httpGet on path /q/health/live
    2. readinessProbe exists with httpGet on path /q/health/ready
    3. Both probes reference configurable values (e.g., .Values.livenessProbe.initialDelaySeconds)
    4. Probe configuration is complete (initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold)
  </action>
  <verify>grep -E "(livenessProbe|readinessProbe|/q/health)" chart/templates/deployment.yaml</verify>
  <done>Health probes configured with /q/health/live and /q/health/ready endpoints using configurable values</done>
</task>

<task type="auto">
  <name>Task 2: Verify configurable resources in values.yaml</name>
  <files>chart/values.yaml</files>
  <action>
    Read chart/values.yaml and verify:
    1. resources section exists with limits (memory, cpu) and requests (memory, cpu)
    2. livenessProbe section exists with all timing parameters
    3. readinessProbe section exists with all timing parameters
    4. Default values are production-appropriate
  </action>
  <verify>grep -E "(resources:|limits:|requests:|livenessProbe:|readinessProbe:)" chart/values.yaml</verify>
  <done>Container resources and probe timing parameters are configurable via values.yaml</done>
</task>

<task type="auto">
  <name>Task 3: Update roadmap to mark Phase 3 complete</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Update .planning/ROADMAP.md:
    1. Change Phase 3 Plans line from "—" to "1 plan (COMPLETE)"
    2. Mark plan as complete: - [x] 03-01-PLAN.md — Verify health probes and configurable resources
    3. Update traceability table: Change HELM-03 and HELM-04 status from "Pending" to "✓ Complete"
  </action>
  <verify>grep -E "(Phase 3|HELM-03|HELM-04)" .planning/ROADMAP.md</verify>
  <done>ROADMAP.md reflects Phase 3 completion with HELM-03 and HELM-04 marked complete</done>
</task>

</tasks>

<verification>
- [ ] deployment.yaml contains livenessProbe with /q/health/live
- [ ] deployment.yaml contains readinessProbe with /q/health/ready
- [ ] deployment.yaml uses configurable values (.Values.livenessProbe.*)
- [ ] values.yaml contains resources section with limits and requests
- [ ] values.yaml contains livenessProbe configuration
- [ ] values.yaml contains readinessProbe configuration
- [ ] ROADMAP.md updated with completion status
</verification>

<success_criteria>
Phase 3 marked complete - requirements HELM-03 (health probes) and HELM-04 (configurable resources) verified as implemented in Phase 1
</success_criteria>

<output>
After completion, create `.planning/phases/03-kubernetes-health-probes/03-01-SUMMARY.md`
</output>

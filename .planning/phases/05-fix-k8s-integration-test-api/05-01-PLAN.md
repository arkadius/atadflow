---
phase: 05-fix-k8s-integration-test-api
plan: 01
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - backend/src/test/java/io/atadflow/integration/KubernetesIntegrationTest.java
autonomous: false

must_haves:
  truths:
    - "KubernetesIntegrationTest.java uses correct Apache Spark K8s Operator API (spark.apache.org/v1)"
    - "K8s integration test runs successfully on k3d cluster and proves end-to-end Spark execution"
  artifacts:
    - path: "backend/src/test/java/io/atadflow/integration/KubernetesIntegrationTest.java"
      contains: "apiVersion: spark.apache.org/v1"
      not_contains: "sparkoperator.k8s.io"
---

<objective>
Fix the SparkApplication API version mismatch in KubernetesIntegrationTest.java and verify the integration test runs successfully on a k3d cluster.

The test currently uses Kubeflow Spark Operator API (sparkoperator.k8s.io/v1beta2) but Phase 2 installs Apache Spark K8s Operator which uses spark.apache.org/v1 API. This causes the test to fail at runtime.

Purpose: Close the gap found during v1.2 milestone audit.
Output: Fixed test file, verified end-to-end execution on k3d.
</objective>

<context>
@.planning/v1.2-MILESTONE-AUDIT.md
@backend/src/test/java/io/atadflow/integration/KubernetesIntegrationTest.java
@.planning/phases/04-k8s-integration-test-documentation/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SparkApplication API version in KubernetesIntegrationTest.java</name>
  <files>
    backend/src/test/java/io/atadflow/integration/KubernetesIntegrationTest.java
  </files>
  <action>
Change line 230 from:
```yaml
apiVersion: sparkoperator.k8s.io/v1beta2
```

To:
```yaml
apiVersion: spark.apache.org/v1
```

This matches the API version used by the Apache Spark K8s Operator installed via Helm in the test setup.

Note: The `kind` should remain `SparkApplication` — this is the same CRD name used by both Kubeflow and Apache operators.
  </action>
  <verify>
1. `grep "spark.apache.org/v1" backend/src/test/java/io/atadflow/integration/KubernetesIntegrationTest.java` returns the fixed API version
2. `grep "sparkoperator.k8s.io" backend/src/test/java/io/atadflow/integration/KubernetesIntegrationTest.java` returns no matches
3. `cd backend && ./gradlew compileTestJava` compiles without errors
  </verify>
  <done>
- API version fixed from sparkoperator.k8s.io/v1beta2 to spark.apache.org/v1
- Test compiles successfully
  </done>
</task>

<task type="manual">
  <name>Task 2: Run K8s integration test on k3d cluster</name>
  <files>
    backend/src/test/java/io/atadflow/integration/KubernetesIntegrationTest.java
  </files>
  <action>
**Manual step requiring k3d cluster:**

Prerequisites (must be set up before running test):
1. Create k3d cluster: `k3d cluster create atadflow-test`
2. Build Docker image: `docker build -t atadflow/atadflow:1.2.0 .`
3. Load image: `k3d image import atadflow/atadflow:1.2.0 -c atadflow-test`

Run the test:
```bash
cd backend
K8S_INTEGRATION_TEST=true ./gradlew test --tests KubernetesIntegrationTest
```

Expected results:
- Health endpoints test passes
- API endpoints test passes
- Full stack flow submission test passes (flow created, job submitted, stayed RUNNING, cancelled successfully)

This proves end-to-end Spark execution on Kubernetes works correctly.
  </action>
  <verify>
1. Test completes without errors
2. All three test methods pass: testHealthEndpoints, testApiEndpoints, testFullStackFlowSubmission
3. Output shows job stayed in RUNNING state (proving Spark execution works)
  </verify>
  <done>
- Integration test passes on k3d cluster
- Full end-to-end Spark execution verified
  </done>
</task>

</tasks>

<verification>
1. API version fixed to spark.apache.org/v1
2. Test compiles successfully
3. Integration test runs on k3d (manual step)
4. All test assertions pass proving end-to-end Spark execution
</verification>

<success_criteria>
- [ ] API version fixed in KubernetesIntegrationTest.java
- [ ] Test compiles successfully
- [ ] Integration test runs and passes on k3d cluster
- [ ] Full lifecycle verified: create flow → submit job → poll status → cancel
</success_criteria>

<output>
After completion, create `.planning/phases/05-fix-k8s-integration-test-api/05-01-SUMMARY.md`
</output>

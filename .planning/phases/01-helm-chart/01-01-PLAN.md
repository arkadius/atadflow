---
phase: 01-helm-chart
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - chart/Chart.yaml
  - chart/values.yaml
  - chart/templates/deployment.yaml
  - chart/templates/service.yaml
  - chart/templates/_helpers.tpl
  - chart/templates/tests/test-connection.yaml
  - chart/.helmignore
  - chart/README.md
autonomous: true

must_haves:
  truths:
    - "Chart installs with 'helm install' and creates PostgreSQL + app pods"
    - "Application connects to PostgreSQL via internal DNS"
    - "Health probes use /q/health/live and /q/health/ready"
    - "Environment variables properly configured from values"
    - "Secrets managed via --set, not hardcoded in values.yaml"
  artifacts:
    - path: "chart/Chart.yaml"
      provides: "Chart metadata with Bitnami PostgreSQL dependency"
      contains: "dependencies:"
    - path: "chart/values.yaml"
      provides: "Default configuration values"
      contains: "postgresql:"
    - path: "chart/templates/deployment.yaml"
      provides: "Application deployment with health probes"
      contains: "livenessProbe:"
    - path: "chart/templates/service.yaml"
      provides: "ClusterIP service for application"
      contains: "kind: Service"
  key_links:
    - from: "deployment.yaml"
      to: "postgresql"
      via: "environment variable DATABASE_URL"
      pattern: "jdbc:postgresql://.*-postgresql"
    - from: "deployment.yaml"
      to: "spark-connect"
      via: "environment variable SPARK_CONNECT_URL"
      pattern: "SPARK_CONNECT_URL"
    - from: "deployment.yaml"
      to: "/q/health/*"
      via: "HTTP health probe"
      pattern: "httpGet.*/q/health"
---

<objective>
Create a Helm chart for Atadflow with PostgreSQL as a chart dependency.

Purpose: Enable Kubernetes deployment of Atadflow with embedded PostgreSQL database, following Helm best practices and Bitnami chart patterns.

Output: Complete Helm chart in chart/ directory with Chart.yaml, values.yaml, templates/
</objective>

<execution_context>
@/home/arkadius/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/arkadius/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-helm-chart/01-RESEARCH.md
@docker-compose.yml
@backend/src/main/resources/application.properties
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Chart.yaml with Bitnami PostgreSQL dependency</name>
  <files>chart/Chart.yaml</files>
  <action>
Create Chart.yaml with:
- apiVersion: v2
- name: atadflow
- version: 0.1.0, appVersion: "1.2.0"
- Bitnami PostgreSQL dependency: version "~18.3.0", repository "oci://registry-1.docker.io/bitnamicharts"
- condition: postgresql.enabled

Reference: 01-RESEARCH.md Pattern 1 for exact YAML structure.
  </action>
  <verify>
`helm dependency build chart/` completes without error
  </verify>
  <done>
Chart.yaml exists with postgresql dependency configured
  </done>
</task>

<task type="auto">
  <name>Task 2: Create values.yaml with full configuration</name>
  <files>chart/values.yaml</files>
  <action>
Create values.yaml with:
- image: repository (atadflow/atadflow), tag, pullPolicy
- replicaCount: 1
- service: type ClusterIP, port 80, targetPort 8080
- postgresql: enabled=true, auth.username=atadflow, auth.database=atadflow, primary.persistence.enabled=true, primary.resources
- spark.connectUrl default
- resources limits/requests for app container
- nodeSelector, tolerations, affinity (empty by default)
- ingress disabled by default

IMPORTANT: Do NOT set postgresql.auth.password - require user to set via --set or external secrets (per anti-pattern in research).
  </verify>
  <verify>
`helm template chart/` renders without YAML errors
  </verify>
  <done>
values.yaml with sensible defaults, no hardcoded secrets
  </done>
</task>

<task type="auto">
  <name>Task 3: Create deployment.yaml and service.yaml templates</name>
  <files>chart/templates/deployment.yaml, chart/templates/service.yaml, chart/templates/_helpers.tpl, chart/templates/tests/test-connection.yaml</files>
  <action>
Create _helpers.tpl with:
- atadflow.fullname
- atadflow.labels (app, chart, release)
- atadflow.selectorLabels

Create deployment.yaml with:
- Standard Kubernetes Deployment spec
- Security contexts (runAsNonRoot, fsGroup)
- Container: image from values, ports (8080)
- Liveness probe: httpGet /q/health/live (per docker-compose.yml pattern)
- Readiness probe: httpGet /q/health/ready
- Environment variables:
  - DATABASE_URL: jdbc:postgresql://{{ .Release.Name }}-postgresql:5432/{{ .Values.postgresql.auth.database }}
  - QUARKUS_DATASOURCE_USERNAME: {{ .Values.postgresql.auth.username }}
  - QUARKUS_DATASOURCE_PASSWORD: from secretKeyRef (postgresql-postgres-password)
  - SPARK_CONNECT_URL: {{ .Values.spark.connectUrl }}
- Resources from .Values.resources

Create service.yaml with:
- ClusterIP type
- Port 80 -> 8080

Create test-connection.yaml with:
- Job that curls the app's /q/health/live endpoint
- ActiveDeadlineSeconds: 60

Reference: 01-RESEARCH.md Code Examples section.
  </action>
  <verify>
`helm template chart/` produces valid Kubernetes manifests with all expected resources
  </verify>
  <done>
Templates render correctly, health probes configured, env vars wired to PostgreSQL
  </done>
</task>

</tasks>

<verification>
1. `helm dependency build chart/` - pulls Bitnami PostgreSQL chart
2. `helm template chart/` - renders without errors
3. Rendered deployment contains:
   - livenessProbe.httpGet.path: /q/health/live
   - readinessProbe.httpGet.path: /q/health/ready
   - env DATABASE_URL with postgresql host
   - env SPARK_CONNECT_URL from values
4. Rendered service exposes port 80 -> 8080
5. No hardcoded passwords in rendered output
</verification>

<success_criteria>
- Helm chart installs with: helm install atadflow ./chart --set postgresql.auth.password=xxx
- PostgreSQL subchart deploys as dependency
- Application deployment includes health probes using /q/health endpoints
- Application can connect to PostgreSQL via internal DNS
- Chart follows Helm best practices from 01-RESEARCH.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-helm-chart/01-SUMMARY.md`
</output>

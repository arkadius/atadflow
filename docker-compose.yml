services:
  postgres:
    image: postgres:16
    container_name: atadflow-postgres
    environment:
      POSTGRES_DB: atadflow
      POSTGRES_USER: atadflow
      POSTGRES_PASSWORD: atadflow
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
